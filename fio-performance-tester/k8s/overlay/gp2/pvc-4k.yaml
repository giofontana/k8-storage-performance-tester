apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vol-to-test-4k-pv-claim
  namespace: fio-perf-test
spec:
  storageClassName: gp2
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi